<article class="block block-{{ configuration.provider|clean_class }} block-{{ plugin_id|clean_class }}">
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  <div{{ content_attributes.addClass('block-content') }}>
    {{ content }}
  </div>
</article>
